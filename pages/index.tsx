import type { NextPage } from 'next'
import { useQuery } from '@apollo/client';

const Home: NextPage = () => {
  return (
    <div>
      <h1>Hello Next.js</h1>
    </div>
  );
}

export default Home
